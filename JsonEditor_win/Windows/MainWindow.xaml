<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="JsonEditor_win.Windows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:JsonEditor_win"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="using:JsonEditor_win.Models"
    mc:Ignorable="d"
    Title="JsonEditor_win">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <CommandBar HorizontalAlignment="Left" Background="Transparent" 
                    IsOpen="False" DefaultLabelPosition="Right">
            <AppBarButton Icon="Add" Label="New" FontSize="16"
                          Command="{x:Bind ViewModel.CreateNewJsonCommand}"/>
            <AppBarButton Icon="Folder" Label="Open" FontSize="16"
                          Command="{x:Bind ViewModel.OpenJsonCommand}"
                          CommandParameter="{x:Bind AppWindow.Id}"/>
            <AppBarButton Icon="Save" Label="Save" FontSize="16"
                          Command="{x:Bind ViewModel.SaveJsonCommand}"
                          CommandParameter="{x:Bind AppWindow.Id}"/>
        </CommandBar>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TreeView CanDrag="False"
                      ItemsSource="{x:Bind ViewModel.Properties, Mode=TwoWay}"
                      SelectedItem="{x:Bind ViewModel.SelectedProperty, Mode=TwoWay}">
                <TreeView.ItemTemplate>
                    <DataTemplate x:DataType="model:JsonProperty">
                        <TreeViewItem ItemsSource="{Binding Children}"
                                      Content="{Binding Name}" FontSize="16" />
                    </DataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>

            <TextBox Grid.Column="1" FontSize="16" TextWrapping="Wrap" Margin="10"
                         Text="{x:Bind ViewModel.Json, Mode=TwoWay}"
                     AcceptsReturn="True"/>
        </Grid>
    </Grid>
</Window>
